// Implementation 1: Using a For Loop

var sum_to_n_a = function(n) {
    let sum = 0;
    for (let i = 1; i <= n; i++) {
        sum += i;
    }
    return sum;
};



// Implementation 2: Using the Arithmetic Series Formula

var sum_to_n_b = function(n) {
    return n * (n + 1) / 2;
};



// Implementation 3: Using Recursion

var sum_to_n_c = function(n) {
    if (n <= 0) {
        return 0;
    }
    return n + sum_to_n_c(n - 1);
};

// Thank you for the opportunity and have a nice day :) 
